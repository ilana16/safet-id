// This utility has been temporarily disabled due to Firebase migration.
// Multi-source medication API functionality will be restored in a future update.

export const searchMultiSourceMedications = async (query: string) => {
  console.log('Multi-source medication search functionality is temporarily unavailable during Firebase migration.');
  return [];
};

export const multiSourceMedicationSearch = async (query: string) => {
  console.log('Multi-source medication search functionality is temporarily unavailable during Firebase migration.');
  return [];
};

export const getEnhancedDrugDetails = async (drugId: string) => {
  console.log('Enhanced drug details functionality is temporarily unavailable during Firebase migration.');
  return null;
};

