// This utility has been temporarily disabled due to Firebase migration.
// Drug API functionality will be restored in a future update.

export const searchDrugs = async (query: string) => {
  console.log('Drug search functionality is temporarily unavailable during Firebase migration.');
  return [];
};

export const searchDrugsCom = async (query: string) => {
  console.log('Drug search functionality is temporarily unavailable during Firebase migration.');
  return [];
};

export const getDrugDetails = async (drugId: string) => {
  console.log('Drug details functionality is temporarily unavailable during Firebase migration.');
  return null;
};

export const getDrugsComUrl = (drugName: string) => {
  console.log('Drug URL functionality is temporarily unavailable during Firebase migration.');
  return '#';
};

