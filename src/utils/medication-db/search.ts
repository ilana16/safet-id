// This utility has been temporarily disabled due to Firebase migration.
// Drug search functionality will be restored in a future update.

export const searchMedications = async (query: string) => {
  console.log('Medication search functionality is temporarily unavailable during Firebase migration.');
  return [];
};

