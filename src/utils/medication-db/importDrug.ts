// This utility has been temporarily disabled due to Firebase migration.
// Drug import functionality will be restored in a future update.

export const importDrug = async (drugData: any) => {
  console.log('Drug import functionality is temporarily unavailable during Firebase migration.');
  return false;
};

