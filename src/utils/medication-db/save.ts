// This utility has been temporarily disabled due to Firebase migration.
// Drug save functionality will be restored in a future update.

export const saveDrug = async (drugData: any) => {
  console.log('Drug save functionality is temporarily unavailable during Firebase migration.');
  return false;
};

