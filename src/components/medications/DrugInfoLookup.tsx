// This component has been temporarily disabled due to Firebase migration.
// Drug lookup functionality will be restored in a future update.

import React from 'react';

const DrugInfoLookup = () => {
  return (
    <div className="p-4 bg-gray-50 rounded-lg">
      <p className="text-gray-600">Drug lookup functionality is temporarily unavailable during the Firebase migration.</p>
    </div>
  );
};

export default DrugInfoLookup;

